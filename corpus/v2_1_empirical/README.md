# NetSniper v2.1 Empirical Characterization Corpus

This directory stores the committed intake contract and sanitized fixture manifest, not raw private scans.

Ground-truth labels must remain separate from classifier inputs. The classifier must never consume expected labels.
