# NetSniper v2.1 Evaluation Candidate Reseal

## Purpose

The embedded-administration boundary changed classifier semantics before the
first evaluation replay. This checkpoint records the finalized classifier
candidate commit `f8b006038bc889266960875c10e765ec0e93ab04` without executing, replaying, or consulting any
evaluation result.

## Sealed candidate

The reseal binds together:

- finalized candidate commit `f8b006038bc889266960875c10e765ec0e93ab04`;
- candidate base commit `809b23432348b6d0dc861948532cb4d5de21da78`;
- superseded candidate commit `a760fc7e125a223181cf44c63a151c3eebcbe460`;
- evaluation-freeze commit `a4c027c9ab5f2fd1e7484b2ae9f8a76b10988c03`;
- the corrected runtime fingerprint index;
- the unchanged evaluation ground-truth and expectation contract; and
- the unchanged 18 prepared synthetic input files.

The dedicated validator reads every sealed runtime and prepared input from the
candidate commit and compares it with the current repository. This ensures the
reseal metadata cannot silently point at a different classifier or holdout.

## No-replay boundary

This checkpoint does not execute an evaluation fixture, import evaluation
results, activate planned fixtures, or permit statistical claims. The first
evaluation replay remains blocked until all three sanitized-real fixtures have
genuine sanitized captures and independently established ground truth.

Run:

```bash
./tools/validate_v2_1_evaluation_candidate_reseal.py
```
