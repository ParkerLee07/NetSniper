# NetSniper v2.1 Sealed Evaluation Corpus Preparation

Status: nine synthetic evaluation inputs prepared; three sanitized-real captures pending.

## Purpose

This checkpoint prepares the evaluation holdout without executing it. The
classifier candidate, frozen expectations, and prepared synthetic inputs are
fingerprinted in `fixtures/device-corpus/evaluation/seal.json` before the first
evaluation replay.

The evaluation split remains a process holdout. Repository visibility is not a
blind-test guarantee, so contributors must not tune classifier rules, weights,
thresholds, or expectations after evaluation outcomes are viewed.

## Prepared and pending fixtures

The checkpoint prepares exactly nine synthetic evaluation fixtures. Each has a
tracked source record and normalized replay input under
`fixtures/device-corpus/evaluation/prepared/`.

The three sanitized-real evaluation fixtures remain planned and artifact-free:

- `sanitized-real-home-router-01`;
- `sanitized-real-printer-01`; and
- `sanitized-real-client-endpoint-01`.

These fixtures may be populated only from genuine captures whose ground truth
was established independently and whose sensitive network identifiers were
sanitized. Synthetic substitutes are prohibited.

## Seal boundary

The seal records:

- candidate commit `a760fc7e125a223181cf44c63a151c3eebcbe460`;
- evaluation-freeze commit `a4c027c9ab5f2fd1e7484b2ae9f8a76b10988c03`;
- hashes of the classifier runtime and evidence contracts;
- a canonical hash of all 12 evaluation ground-truth and expectation records;
- hashes of the 18 prepared synthetic input files;
- the three missing genuine-capture blockers; and
- confirmation that no evaluation replay has occurred.

Any classifier, evidence-profile, evaluation expectation, or prepared-input
change invalidates the seal and requires an explicit baseline reset before
release evaluation.

## No-result preparation gate

Run:

```bash
./tools/validate_v2_1_evaluation_preparation.py
```

This validator performs structural, confinement, anti-leakage, fingerprint,
holdout, and no-results checks. It deliberately does not classify or replay any
evaluation fixture.

The existing development and regression corpus remains the only replayed set at
this checkpoint.

## First evaluation replay

The first evaluation replay remains blocked until all three sanitized-real
fixtures have genuine source evidence, normalized evidence, independent ground
truth provenance, and recorded timestamps.

Before that replay:

1. verify the seal against the candidate runtime;
2. activate all 12 evaluation fixtures in one reviewed checkpoint;
3. record a new complete input index including the sanitized-real captures;
4. execute the evaluation once against the sealed candidate;
5. store the immutable result report without changing the candidate; and
6. treat failures as release blockers rather than tuning feedback.

The 12-case starter evaluation set is too small for statistical accuracy claims.
It may support only corpus, regression, compatibility, and safety-gate claims.

## Candidate reseal after semantic correction

The embedded-administration boundary changes classifier semantics before the
first evaluation replay. The prepared inputs, frozen expectations, and 14/12/4
split remain unchanged. The seal now fingerprints the corrected runtime in a
`candidate_reseal_required` state and adds a hard execution blocker. Evaluation
must not run or be used to tune the correction. A later post-commit reseal must
record the finalized classifier commit before evaluation can proceed.
